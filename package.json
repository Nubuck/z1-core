{
  "name": "@z1/core",
  "version": "0.3.1",
  "description": "Z1 Core Mono Repo",
  "main": "index.js",
  "author": "@Nubuck",
  "license": "MIT",
  "private": false,
  "scripts": {
    "lerna": "lerna",
    "clean": "lerna clean",
    "clean:presets": "lerna clean --scope=@z1/preset-*",
    "clean:libs": "lerna clean --scope=@z1/lib-*",
    "clean:kits": "lerna clean --scope=@z1/kit-*",
    "clean:examples": "lerna clean --scope=example-app-*",
    "list": "lerna ls",
    "dep": "lerna add",
    "boot": "lerna bootstrap --force-local",
    "boot:presets": "lerna bootstrap --force-local --scope=@z1/preset-*",
    "boot:libs": "lerna bootstrap --force-local --scope=@z1/lib-*",
    "boot:kits": "lerna bootstrap --force-local --scope=@z1/kit-*",
    "boot:examples": "lerna bootstrap --force-local --scope=@z1/example-app-*",
    "publish": "lerna publish",
    "examples": "nx run-many --target=start --all",
    "build": "nx run-many --target=build --all --nx-bail"
  },
  "dependencies": {
    "lerna": "3.19.0"
  },
  "devDependencies": {
    "nx": "14.4.3"
  }
}
