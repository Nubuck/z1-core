import { task } from '@z1/preset-task'
import { Nedb, FeathersNedb } from '@z1/preset-feathers-server-nedb'

// main
const createModel = task(t => (name, props, options = {}) => () => {})
const createDBConnection = task(t => app => {})
