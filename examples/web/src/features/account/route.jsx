import { featureBox as fbx } from '@z1/lib-feature-box'

// main
// export const route = ctx => {
//   return fbx.ui.connect(
//     fbx.ui.query(['account', 'location']),
//     ctx.mutators
//   )(({ location, routing }) => {
//     return <div>{fbx.routing.render(location.type, routing)}</div>
//   })
// }
