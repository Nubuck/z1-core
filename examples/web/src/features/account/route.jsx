import zbx from '@z1/lib-feature-box'

// main
// export const route = ctx => {
//   return zbx.ui.connect(
//     zbx.ui.query(['account', 'location']),
//     ctx.mutators
//   )(({ location, routing }) => {
//     return <div>{zbx.routing.render(location.type, routing)}</div>
//   })
// }
